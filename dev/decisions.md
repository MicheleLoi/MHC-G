# Decisions Log

<!-- One line per decision. Format:
     YYYY-MM-DD | [decision] | [file affected] | [rationale] -->

## Germinal

<!-- These record the decisions that brought this file into existence.
     They travel with every copy so the fossil record begins at its own origin. -->

2026-03-12 | decisions.md created as the fossil record of the genome's evolution | dev/decisions.md | A genome that changes without a visible trace cannot be audited or understood by someone arriving later. One line per decision makes the trace scannable without requiring design-document literacy.
2026-03-12 | Format: one line per decision, pipe-delimited — date | decision | file | rationale | dev/decisions.md | Scannability over completeness. The log is a pointer and a reason, not a transcript. Full reasoning lives in DESIGN.md and working/.
2026-03-12 | dev/decisions.md ships (crosses the membrane) | DESIGN.md | The fossil record is part of what makes the system trustworthy to someone encountering it for the first time. If only the genome ships but not the trace of how it was shaped, the artifact does not instantiate its own transparency principle.

## Log

2026-03-12 | Replaced "exactly 3 rules" ceiling with "as few as pass the generalizability check; current count is 3" | CONTRIBUTING.md | The ceiling was protecting a number, not a principle. The generalizability check is the real friction mechanism. The ceiling blocked well-founded additions without adding value beyond what the check already provides.
2026-03-12 | Governance artifacts (session-log.md, decisions.md) moved from prototype root to project folder; dev sessions use dev/ | CLAUDE.md, CONTRIBUTING.md | Keeps the prototype as a portable stateless toolkit. User state stays with the project. Dev state stays in dev/. Separates tool from content in all use cases.
2026-03-12 | dev/ established as the dev project folder — no sibling dev folder needed | guide.md, CONTRIBUTING.md | dev/ already holds DESIGN.md and traces/. Adding session-log.md and decisions.md there completes it as the dev workspace. A sibling folder would duplicate the role.
2026-03-12 | guide.md gains a dev redirect at the top | guide.md | Prevents developers from following normal-user setup and creating an unnecessary sibling project folder — the error that prompted this session.
2026-03-12 | Rule 4 (Persist) added | CLAUDE.md | Generalizes to all projects: work products should persist in files, not chat. Defaults (session-log.md, decisions.md) are offered on first use. Structure beyond defaults is user-shaped, not prescribed. Facilitating, not directive.
2026-03-12 | All inputs to a process go into dev/working/ — not held only in Claude's context | dev/working/ | Inputs (uploaded files, shared conversations, reference materials) must be accessible alongside outputs. A process whose inputs are invisible cannot be audited or resumed.
2026-03-12 | Rule 4 extended to cover source materials alongside work products | CLAUDE.md | "Work products and source materials go into files" — inputs must persist too, not just outputs. Generalizes to all projects. Named folder convention (inputs/ vs outputs/) not prescribed — left to user via Rule 4's "help shape" clause and a guide.md tip.
2026-03-12 | Rule 5 (Cohere) added | CLAUDE.md | Generalizes to all projects: conventions shaped through use must be maintained consistently. Without this, Rule 4's adapt layer would drift. Passes generalizability check: naming, structure, and labelling conventions emerge in every sustained project regardless of domain.
2026-03-12 | Rule 4 rewritten as transparency principle | CLAUDE.md | Previous Rule 4 prescribed location (project folder) and low-level behaviors (date in headers, input tracing). These are implementation details, not a principle. The principle that generalizes is: what persists should be visible to the user. Location is now governed by DESIGN.md's structure. Disclosure at first persistence is the behavioral default.
2026-03-12 | Records and adaptations consolidated into the prototype's projects/; project folders contain work products only | CLAUDE.md, DESIGN.md | The prototype is a living instance, not a stateless toolkit. User state (records, adaptations) lives inside it, gitignored. Project folders stay clean. Mirrors how Claude Code's .claude/ directory works. Portability preserved via gitignore.
2026-03-12 | adapt layer introduced: adapt.md at root (user-level), projects/[name]/adapt.md (per-project), dev/adapt.md (dev-specific) | DESIGN.md, CONTRIBUTING.md | Operational instructions that don't generalize to all users need a first-class home. Without adapt layer, non-core adaptations either contaminate the genome or disappear into rationale documents where they can't function operationally.
2026-03-13 | Cell architecture adopted: all project work moves inside projects/[name]/ | CLAUDE.md, DESIGN.md, guide.md | Projects are cells. The cell contains everything — inputs, drafts, records. Nothing lives outside the cell except exported deliverables. This eliminates the split between prototype state and the project folder, which was architecturally redundant once records were already inside the prototype.
2026-03-13 | _records/ subfolder introduced inside project folders | CLAUDE.md, DESIGN.md | Work files (drafts, notes, inputs/) live at the root of the project folder where the user finds them immediately. System records (session-log, decisions, adapt) live in _records/. Underscore prefix sorts records below work files in most file browsers without hiding them — consistent with the disclosure principle.
2026-03-13 | Secretion defined as explicit export: Claude exports deliverables on request in requested format | CLAUDE.md, DESIGN.md, guide.md | Deliverables are not written to an external project folder by default — they are produced inside the cell and exported when ready. Keeps markdown source inside the cell; external artifacts are conversions. Pandoc is available in Cowork's Debian environment for format conversion.
2026-03-13 | Reproduction mechanism: germ-line and differentiated instantiation | CLAUDE.md, DESIGN.md | New projects need a defined initiation flow. Germ-line: fresh start, user-level adapt.md inherited silently. Differentiated: inherits _records/adapt.md from a named parent project. Claude asks for project name first, offers differentiation only if other projects exist. Max two questions; everything else disclosed after.
2026-03-13 | inputs/ folder introduced as first-class location for internalized source materials | CLAUDE.md, DESIGN.md | External files (PDFs, docs, references) are internalized into markdown and placed in inputs/ at project root. The cell absorbs inputs rather than linking to them. Keeps external dependencies inside the cell boundary.
2026-03-13 | Germinal section added to dev/decisions.md — founding entries that record the file's own origin | dev/decisions.md, CONTRIBUTING.md | The artifact should instantiate its own principles. A transparency log that has no trace of its own creation violates the principle it embodies. Germinal entries ship with every copy so the fossil record begins at its own origin.
2026-03-13 | Renamed prototype → MHC-G throughout all files | CLAUDE.md, DESIGN.md, guide.md, CONTRIBUTING.md | G = Germinal. This is the founding cell, the first of its species. The name marks its identity as such.
2026-03-13 | Cell selection principle: user selects MHC-G directly in Cowork, not a parent folder | CLAUDE.md, DESIGN.md, guide.md, CONTRIBUTING.md | The cell IS the workspace. Selecting a parent folder was an artifact of an earlier design where the prototype was a subfolder of a larger workspace. With all project work inside MHC-G/projects/, the parent folder has no purpose. Selecting MHC-G directly also removes the MHC-G/ prefix from all internal path references — simpler and more correct.
2026-03-13 | MHC-G/ prefix dropped from all internal path references | CLAUDE.md, DESIGN.md, guide.md | projects/[name]/ not MHC-G/projects/[name]/. When the cell is the workspace, its own name is implicit. Naming it in every path was redundant and would break if the cell were renamed.
2026-03-13 | Reproduction mechanism without git: genome-only folder or zip | CLAUDE.md, DESIGN.md, guide.md | Git is not available in Cowork and is not appropriate for the target user. Reproduction is: extract genome files (CLAUDE.md, guide.md, CONTRIBUTING.md, dev/DESIGN.md, dev/decisions.md) into a new folder. User state does not travel. The result is a new inert cell. Zip is optional. No dependency on git or any process beyond file copying.
2026-03-13 | dev/adapt.md created as the individual layer for the founding cell | dev/adapt.md | This cell is tracked in git; others may not be. Shipping instructions for Claude Code belong here, not in the genome — they are specific to this individual and would not generalize to other cells. dev/adapt.md does not travel with copies.
2026-03-13 | Session Start extended: briefly surface state at session open (what was last worked on, what's next); step 4 added — proactive continuity readiness mid-session | CLAUDE.md | Recovery behaviors (read session log, show decisions, start fresh) were documented in guide.md as user commands but not in CLAUDE.md as Claude behaviors. Claude must offer them without the user knowing to ask. The guide documents what's possible; CLAUDE.md makes Claude ready to do it unprompted.
2026-03-13 | Development section extended: also read dev/session-log.md; orient developer before asking what to work on | CLAUDE.md | Arriving at a dev session cold — knowing only DESIGN.md and CONTRIBUTING.md — leaves the developer without current state. Reading the session log and surfacing version + open questions closes the gap. Generalizes to any developer on any fork.
2026-03-13 | dev/adapt.md extended with Developer sessions in Cowork section | dev/adapt.md | adapt.md previously covered only Claude Code shipping. A developer opening this cell in Cowork had no operational orientation from adapt.md. The new section covers: what to read, how to orient, editing protocol reminder, continuity readiness. Individual-layer because any fork's developer sessions may differ.
2026-03-13 | Rule 3 extended — offer to save standalone artifacts as named documents | CLAUDE.md | Generalizes: any session can produce reference material worth preserving independently of the session log. The log container is insufficient for artifacts that have value beyond the session narrative. Wording reviewed with Opus 4.6.
2026-03-16 | Design principle 6 (AI-congruent governance) added to DESIGN.md | dev/DESIGN.md | MHC-G's rules are principle-shaped rather than procedure-shaped because principle-based governance is congruent with the cognitive profile of language models. The predecessor system (v1–v3.37) grew by adding rules to patch each failure — a compliance architecture that placed the AI in conditions of poor cognitive fit. MHC-G inverts this: boundary conditions + native model strengths. The biological framing is load-bearing, not decorative. This principle was always implicit in the design; making it explicit ensures it survives as a design constraint against future rule-accretion.
2026-03-17 | Session Start extended: read adapt.md silently if present, before checking session log | CLAUDE.md | adapt.md holds cross-project defaults that should apply from the first action of a session. Reading it after the session log would mean those defaults arrive too late to govern the opening orientation.
2026-03-20 | CLAUDE.md token compression planned but deferred | dev/working/claude-md-compression.md | CLAUDE.md is loaded every session — compressing it saves tokens on every conversation. Plan saved to dev/working/ for execution in a future session.
2026-03-20 | dev/DESIGN.md compressed ~2.5% (274 → 267 lines); no semantic changes, no version bump | dev/DESIGN.md | Compression edit only: redundant framing, duplicated restatements, and over-long closing sentences tightened. Version and date unchanged because no design decision was altered.
2026-03-20 | Opus 4.6 used as judge for CLAUDE.md compression — verdict: ~5–8% safe cuts, not 15–20% | CLAUDE.md | Most verbosity in CLAUDE.md is functional: it prevents known model failure modes. Verbal precision aids compliance. Only passages where words were redundant with structure or pure human-facing motivation were removed.
2026-03-20 | Four safe compression changes applied to CLAUDE.md; Rule 1 second paragraph left untouched | CLAUDE.md | Safe changes: Rule 3 dev/ parenthetical factored out; Session Start step 1 asides removed; Workspace restatement cut; Reproduction motivational aside removed. Rule 1 second paragraph uncertain (80% safe per Opus) — left as is to avoid performance risk.
2026-03-20 | All standalone MHC references excised from genome files | dev/DESIGN.md, guide.md, dev/decisions.md | Predecessor system not yet public; named references create unexplained loose threads. Predecessor lesson in DESIGN.md kept but anonymized — the example teaches without the name.
2026-03-20 | README pointer to guide.md added | README.md | No path existed from README to guide for users who download the repo. Opus recommended a two-line pointer (Option A) over lifting content — README brevity is load-bearing.
2026-03-23 | Test protocol written as human-executable scenario script | dev/working/test-protocol.md | MHC-G has no code to unit-test; compliance is observable only in conversation. Scenario script with pass criteria is the appropriate form for a conversational governance system.
2026-03-23 | Structural criteria automated via structural-check.py | dev/working/structural-check.py | File existence, session-log fields, decisions format, and artifact presence are mechanically checkable without human judgment — separating them from judgment-heavy criteria reduces tester burden.
2026-03-23 | Agent simulation built: journalist user agent + MHC-G agent + judge | dev/working/sim-agent/sim.py | Enables stress-testing behavioral rules under adversarial conditions (impatient user, "whatever you think" dismissals) that the manual protocol's cooperative tester cannot reproduce.
2026-03-23 | MHC-GT created as genome-only test cell | dev/working/sim-agent/MHC-GT/ | Simulation must test a fresh cell, not a developer cell with adapt.md and dev state. MHC-GT contains only the five genome files.
2026-03-23 | No delete tool in simulation | dev/working/sim-agent/sim.py | MHC-G targets Cowork where rm is blocked. Granting delete to the simulated agent would test a system different from the one in production.
2026-03-24 | security.md added to genome; travels with copies | security.md, CLAUDE.md, dev/DESIGN.md, guide.md | User feedback: "saying it's safe has no meaning — write specific claims with ways to verify them." Four verifiable claims (folder access scope, no auto-executing scripts, CLAUDE.md readable in full, files stay local), a note on sensitive files, and an honest statement of what MHC-G doesn't protect against (prompt injection from untrusted content). Own file rather than section in guide.md — sufficient substance to stand alone.
2026-03-25 | START-HERE.txt added to genome; travels with copies | START-HERE.txt, guide.md | Real user (non-technical, daily ChatGPT user) could not open .md files and selected the wrong folder after ZIP extraction. Plain .txt is universally openable on Windows without prompting for a program. Covers two failure modes: what .md files are and how to open them; ZIP nesting producing MHC-G/MHC-G/.
2026-03-25 | guide.md setup section extended with "Before you start" preamble | guide.md | Explains that .md files are plain text, how to open them, and the ZIP nesting trap. Placed before "Open in Cowork" so it's the first thing a new user reads after the introduction.
2026-03-25 | Assumed-user gap documented in DESIGN.md Target User section | dev/DESIGN.md | "Not a technical user" assumed file-system literacy (opening .md, navigating ZIP extraction). Tested against a real non-technical user and failed. Gap named explicitly: AI literacy and file-system literacy are different populations. Distribution format added as open backlog item.
2026-03-29 | Rule 5 (Cohere) demoted from rule to operational instruction in Workspace section | CLAUDE.md, dev/DESIGN.md | Convention maintenance (naming, structure, labelling) is baseline professional competence, not a governance principle at the same level as the decision-loop rules (1–3), persistence (4), or complementarity (new 5). The instruction is preserved in CLAUDE.md's Workspace section; it does not disappear, it moves down a level. Rule count stays at 5.
2026-03-29 | New Rule 5 (Complement) added: when stuck, name the difficulty and ask for help | CLAUDE.md, dev/DESIGN.md, guide.md | Generalizes to all projects: AI stubbornness (looping, persisting on failing approaches) is a known failure mode across domains. The rule addresses it by requiring escalation to the human, grounded in the principle that human and AI have different strengths. Trigger is failure cases only (stuck, looping, no progress) — proactive handoff for non-failure cases is already covered by Rule 2 (surface meaningful choices). Passes generalizability check: a recipe blog, legal brief, and field research log all benefit from an AI that asks for help instead of spinning.
2026-03-30 | Installers updated with folder-choice prompt; Desktop default, drag-and-drop alternative | dev/working/installer/install-mac.command, dev/working/installer/install-windows.bat | Hardcoded Desktop destination was too rigid. Drag-and-drop into terminal is supported on both macOS and Windows and avoids requiring non-technical users to type paths. Desktop remains the default (just press Enter) to keep the easy path easy.
2026-03-31 | Rule 3 trigger widened from session-end to "substantial ground covered"; prompt reworded from "what we did today" to "what we've done so far" | CLAUDE.md, guide.md | The end-of-session framing caused users to decline the write-up offer mid-session because accepting felt like closing the session. Context compaction then erased the detail that the log would have preserved. The fix is framing, not mechanism: "so far" signals progress-saving, not wrap-up. guide.md parenthetical added to make this explicit to the user.
2026-03-30 | Rule 5 (Complement) rewritten: observable trigger replaces abstract self-diagnosis | CLAUDE.md, dev/DESIGN.md, guide.md | The v1.7 wording ("when you are stuck") required the model to diagnose its own state — but a model that is looping does not recognise it is stuck, because it is always generating the next attempt. The rewrite gives an observable trigger ("when your second attempt fails the same way") that the model can pattern-match on before the keep-trying instinct takes over. Adds a forward-looking element ("what you would try next") so the human receives a proposal, not just a problem — mirroring Rule 1's structure. This is the one rule that is deliberately more concrete than the others, because it is the one rule that fights the model's default behaviour rather than shaping it. Generalises: recipe blog, legal brief, field research log all benefit from an AI that stops after two identical failures instead of spinning.
